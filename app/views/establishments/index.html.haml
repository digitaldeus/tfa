%p#notice= notice
%h1 Listing Establishments
%table
  %thead
    %tr
      %th Name
      %th Description
      %th{:colspan => "3"}
  %tbody
    - @establishments.each do |establishment|
      %tr
        %td= establishment.name
        %td= establishment.description.html_safe unless establishment.description.nil?
        %td
          = link_to 'Show', establishment
          |
        %td
          = link_to 'Edit', edit_establishment_path(establishment)
          |
        %td= link_to 'Destroy', establishment, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Establishment', new_establishment_path
